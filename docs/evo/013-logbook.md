# EVO-013: Logbook —— 一次运行的航海日志

日期：2026-07-28（同日修订，见下）

状态：**设计已收敛，可落代码。** 本文是契约文档，不是实现规范。

> 本文接 [EVO-011](011-perception-observation.md) / [EVO-012](012-continuous-acquisition.md)。那两篇把"帧怎么产生、怎么送到消费者手上"定完了，并且都把**记录**明确排除在范围外（EVO-011 §5.4、EVO-012 §4.2）。本文补上这一层：**发生的事怎么写下来。**

> **修订 · 2026-07-28（初稿之后又聊了一轮，设计明显收窄）**
>
> 初稿把记录层写厚了，隐含着"把 pluck 的 `RunLogger` 一般化搬进框架"。这一轮把它**削到底座**：
>
> 1. **框架只做三件事**（§2.5 新增）：一次运行的册子；**写一行**（同步、绝不丢）；**写大载荷**（异步、可丢、记账）。`event` / `episode` / 四值结局那类大而全接口**明确不做**——**框架提供底座，pluck 在上面自己包挑毛专用的一层**。
> 2. **「小的不丢、大的可丢」升格为框架级规则，且调用方不用选**（§3 规则 5 重写）——递给 `write` 什么东西，就走哪条路。
> 3. **定时：框架完全不管**（§2.4 改写）。`TrajectoryRecorder` 由"框架给出的答案"**降级成一个可选的现成件**。
> 4. **配置驱动照既有约定**；`root` 必填、框架不给默认值（§3 新增规则 8）。
> 5. **跨运行产物 + 按天清理，从"本轮不做"改为"要做"**（§5.1；原 §6.2 相应改写）。
> 6. **可变上下文挂在 `Logbook` 上，不挂 `Scribe`**（§2.6 新增）。
>
> 受影响章节：§0、§2.4、§2.5（新增）、§2.6（新增）、§3（规则 1 补足论证、规则 5 重写、新增规则 8）、§5、§6.2、§7。
> **不变**：两个对象、唯一动词 `write`、`telemetry`→`logbook` 改名与 `SCHEMA_ID` 的处理、框架不解释含义、分账本、丢了记账、pluck 一行不动。

前置文档：
- [EVO-011: 感知层 —— Observation 作为一等公民](011-perception-observation.md) — `Observation` 是被记录的东西之一
- [EVO-012: 连续采集](012-continuous-acquisition.md) — **有界队列 + 丢帧策略 + 记账**由它定义，本文的规则 5/7 直接复用，不重新发明
- [architecture.md](../architecture.md) — 「单一节拍源」；本文的 `Scribe` 刻意站在这条约束之外（它不踩节拍）

**引用基准**：AutoWeaver 为 `main @ ada10ac`；pluck-hair 为 `origin/servo_v5 @ 53e5c3a`。

> ⚠️ **基准提示**：pluck 的 `origin/servo_v5` 在最近几天移动得很快——EVO-012 写作时基准是 `c353f46`，本文写作时已到 `53e5c3a`。**本仓库的 pluck 本地 checkout（`ffcb725`）落后 origin 20+ 个提交**，行号对不上。本文所有 pluck 引用一律取自 `git show origin/servo_v5:<path>`。

---

## 0. 一句话现状

**"把发生的事写下来"这件事，框架只提供了四分之一，于是每个项目自己造了一遍。**

现在有四份实现在做同一件事（一份在 AutoWeaver，三份在 pluck），而**跟 PLC 之间的整条通信流程根本没有落盘**——它只在终端打一行字，关掉窗口就没了。

本轮立起来的是一个**薄底座**：**`Logbook`（一次运行的册子）+ `Scribe`（绑定账本的写入口）+ 唯一动词 `write`**，并把 `telemetry/` 改名为 `logbook/`。

**框架只做三件事**（§2.5）：管好一次运行的册子；**写一行**（同步、绝不丢）；**写大载荷**（异步、可丢、记账）。**除此之外一概不做**——`event` / `episode` / 四值结局那类接口留给业务在底座上自己包。**pluck 侧代码完全不动。**

---

## 1. 起因：四份重复，加一块空白

### 1.1 四份实现，一个想法

| 谁 | 什么时候写 | 写什么 | 写到哪 |
|---|---|---|---|
| `TrajectoryRecorder`（AutoWeaver） | 每 tick | board 上指定 namespace 的**所有**声明键 | `<name>-<session>-NNN.jsonl` |
| `RunLogger.start_pose_sampler`（pluck） | 自己的线程，20 Hz | `arm.pose` 的六个浮点 | `trajectory.jsonl` |
| `RunLogger.event`（pluck） | 调用方主动 | 调用方给的字段（+ 可选图片） | `events.jsonl` + `frames/` |
| `AsyncSampleWriter`（pluck） | 调用方主动 | 一帧像素 | `samples/**/NNNN_*.png` |

**四份各写各的，代码互相不认识。** pluck 完全没有引用 `autoweaver.telemetry`（全仓零命中），它自己又写了一份。

### 1.2 ⚠️ 但它们**不是**同一份代码 —— 差异本身是设计输入

这一条与最初的判断不符，值得写下来：`TrajectoryRecorder` 和 pluck 的 `start_pose_sampler`
虽然在做同一件事，**设计上却处处不同**：

| | `TrajectoryRecorder`（`telemetry/trajectory.py:226`） | `start_pose_sampler`（pluck `runlog.py:592`） |
|---|---|---|
| 谁驱动 | **tick**（`on_tick`，是个 Worker） | **自己的 daemon 线程**（`trajectory_hz`） |
| 取数方式 | `board.snapshot()`，拉整块 | 调用方传进来的 `read_pose()` callable |
| 写什么 | 该 namespace 下**每一个**声明键，原样 | **手挑的六个浮点**（x/y/z/rx/ry/rz） |
| 业务上下文 | **无** | 每行都盖 `phase` / `round` / `attempt` |

两条结论：

1. **"谁来定时"没有统一答案，而且不该有。** 一个由 tick 驱动，一个由后台线程驱动——两种都合理（tick 保证与 BT 对齐；线程能跑到 board 发布频率之外）。这是 §2.4 那条决定的**全部依据**：**定时，框架完全不管**——它无从知道某个业务该用哪种，替它选就是猜。
2. **`TrajectoryRecorder` 做不到 pluck 那份做的事**——它没法在行上盖业务上下文（`phase`/`round`/`attempt`）。这是 §3 规则 2 要一般化的能力，也是 pluck 当初不复用它的合理原因之一。

**所以"重复造轮子"这个说法要收一收**：确实重复，但不是简单抄袭，而是**框架那份不够用**。

### 1.3 完全空白的一块：PLC 通信

跟 PLC 之间一次完整来往有 8 个步骤（收到请求 → 写寄存器 → 给出应答 → 拉旗 → 清旗 → 写标志位 → PLC 取走应答 → 本轮完成），外加两类异常（读失败重连、等待超时提醒）。

这 8 步**每一步都已经算出了耗时**，并且已经在终端打出来了——`comm_engine.py:118`（`✔ 完成, 耗时 %dms`）、`:193`（`← 收到 PLC 请求 func=%s, 等了 %dms`）、`:196`（`✔ PLC 已消费应答, 等了 %dms`）。

**但一个字都没落盘。** `comm_engine.py` / `plc_arm.py` / `plc_modbus.py` 三个文件对 `RunLogger` **零引用**（三个文件逐一 grep 核实，均为 0 处）。

> ⚠️ **行号提示**：上面这几个行号取自 `origin/servo_v5 @ 53e5c3a`。同一批日志点在**本地落后的 checkout** 里是 `:113` / `:169` / `:172`——讨论过程中引用过后者。**以 origin 为准。**

后果是几个基本问题答不上来：挑一根毛约 15 s，**其中多少花在干等 PLC**？289 个视野跑完要多久，瓶颈在哪？

> **这一块是记录层最直接的兑现点**：数已经算好了，缺的只是一个写入口。

---

## 2. 已达成的结论

### 2.1 anchor：航海日志

> **一次航行 = 一本日志。** 值班员往上面写三种东西：**定时记的**（每小时记一次位置、航速）、**出事记的**（改航向、遇到别的船、机器故障）、**附件**（海图、照片，夹在里面）。**每一条都带时间。**

对应关系：

| 航海日志 | 我们 |
|---|---|
| 一次航行一本日志 | **一次运行一个目录** |
| 每小时记位置 | 定时抄 board（臂在哪） |
| 出事记一笔 | PLC 来往、挑毛决策 |
| 夹在里面的照片 | 采集的关键帧 |
| 每条都有时间 | **统一时钟**（所有文件能对到一条时间线上） |

选这个 anchor 而不是继续叫 `telemetry`，理由很简单：**`telemetry` 是个技术词，`logbook` 能想象出画面**——你知道一本航海日志长什么样、谁往上面写、写完放哪。这直接决定了下面两个对象的形状。

### 2.2 两个对象

- **`Logbook`** —— 这次运行的**册子**：一个**目录** + 一个**统一时钟** + 这次运行的**身份**（git sha + dirty、config 指纹、machine id、batch）。
- **`Scribe`** —— **绑定到某一本账本的写入口**。几个 scribe 共用一本 logbook：PLC 那边的往 `plc.jsonl` 写，视觉那边的往 `events.jsonl` 写。

### 2.3 `Scribe` 差点被砍 —— 它挣到位置的**唯一**理由

前车之鉴是 EVO-011 §3.1 的 `Observatory`：那个角色被否决，理由是"**它只是按 role 索引的一个 dict + 装配顺序，那是 `main.py` 的活，不是领域角色**"。

`Scribe` 面临一模一样的质疑：**如果只有一本册子，直接 `logbook.write(...)` 不就完了，要书记员干嘛？**

**它挣到位置的理由只有一条**：

> **一本册子上有多本账本，而写的人通常固定只写其中一本。** `Scribe` 就是"**账本已经选好了的写入口**"——PLC 的 scribe 永远写 `plc.jsonl`，视觉的永远写 `events.jsonl`，调用处不必每次重复指定。

**把这条理由记牢**：它是 `Scribe` 存在的全部依据。如果将来发现绑定的只有账本名这一样东西，那它就退化成一个 currying，届时该重新审视（见 §7）。

### 2.4 唯一动词 `write`；`Scribe` 是**纯被动**的

**只有一个动词。** 写什么取决于你递给它什么——一笔事件、一份观测、一块 board 快照。

这一条顺手解决了一个结构上的别扭。初稿曾把"定时抄 board"当成 `Scribe` 的一种功能，于是 `Scribe` 就得**一只脚踩在节拍上**（要被 tick 广播才能定时），另外两种写法却不需要——一个角色半只脚在 kernel 里，半只脚在外面。

**改成"别人按节奏来叫他写"之后，这个别扭没了：**

- `Scribe` **不踩节拍、不自己开线程、不需要是 Worker**。
- 谁来定时是**调用方的事**。

**修订（2026-07-28）：这一条后来被推到底 —— 定时，框架完全不管。**

初稿只说"定时不是 `Scribe` 的职责"，并在 §7 自评说这"**只是把问题移出去了，没有解决**"——因为谁来定时仍然没有统一答案（tick 还是后台线程）。

**现在的答案是：移出去就是答案。** 框架不替业务选节奏。

理由在 §1.2 的实测对照里已经给全了：`TrajectoryRecorder` 由 tick 驱动，pluck 的 `start_pose_sampler` 由自己的后台线程驱动，**两种都正当**——tick 保证与 BT 对齐；线程能跑到 board 发布频率之外、也不受 tick 抖动影响。**框架无从知道某个业务该用哪种**，替它选就是猜。

于是：

> **"每隔多久抄一次"是业务的事。业务自己开线程（或挂在自己的 Worker 的 tick 上）调 `write`。**

**`TrajectoryRecorder` 因此降级** —— 它从"框架给出的定时记录答案"变成**一个可选的现成件**：如果你的场景恰好是"按 tick 记录 board 上某几个 namespace"，拿去用；不合适就自己写一条线程调 `write`，框架不认为前者更正统。

**这同时让 `telemetry` 那条法律继续成立**：`telemetry/__init__.py:1-7` 原话——

> "Observability for AutoWeaver — **passive board consumers, not control**. ... **Keep it that way — recording is downstream of the kernel, not part of it.**"

一个不踩节拍、不写 control state 的 `Scribe`，天然在 kernel 下游。

### 2.5 框架**只做三件事** —— 底座，不是 `RunLogger`

**（2026-07-28 新增。初稿隐含着"把 pluck 的 `RunLogger` 一般化搬进框架"，这一条明确否掉那个方向。）**

框架提供且**只提供**：

1. **一次运行的册子** —— 一个目录 + 双时钟 + 身份 + 可变上下文（§2.6）；外加跨运行产物的落点与按天清理（§5.1）。
2. **写一行** —— 同步、刷盘、**绝不丢**。
3. **写大载荷** —— 异步、有界队列、**可丢、丢必记账**（复用 `sensor/delivery.py`，见规则 5）。

**明确不做**：`event()` / `episode` / 四值结局（plucked / pluck_failed / abandoned / uncertain）/ "看到几根毛"这类接口。

**为什么**：那些接口一旦进框架，框架就得开始理解业务——而规则 3 刚说过它不该理解。pluck 的 `RunLogger` 是**一层长在底座之上的挑毛专用封装**，它有 episode ledger、有结局映射、有 `recognize`/`poke`/`verdict` 的概念，这些**恰恰是它有用的原因，也恰恰是它不该被一般化的原因**。

> **框架提供底座，pluck 在上面包挑毛专用的一层。** 迁移时 `RunLogger` 不是被删掉，而是**被掏空**——底下换成 `Logbook` + `Scribe`，上面那层业务概念原样留在 pluck。

### 2.6 可变上下文挂在 `Logbook` 上，**不挂 `Scribe`**

**（2026-07-28 新增。）**

运行过程中有一份会变的上下文——pluck 用的是 `phase` / `round` / `attempt`（`runlog.py:431` 的 `set_context`），意思是"**现在流程走到哪了**"。它需要被盖在很多行上。

**它属于 `Logbook`，不属于 `Scribe`**，理由是所属关系：

> **"现在流程走到哪"是整次运行的属性，不是某一本账本的属性。**

而且多个 scribe（PLC 的、视觉的）以及业务自己那条定时线程**都要用到同一份**。挂在 `Scribe` 上就会出现"改了一个 scribe 的上下文，另一个还是旧的"这种必然出错的局面。

⚠️ **键名由业务定，框架绝不写死 `phase` / `round` / `attempt`。** 那三个词是挑毛流程的概念，不是通用概念——框架只提供"一份会变的键值对，写每一行时自动盖上"，至于里面装什么，同规则 3。

---

## 3. `write` 的八条规则

### 规则 1：自动盖时间，调用方不用传

每一笔记**两个**时间：`t`（本次运行开始至今的秒数，单调）+ `wall`（墙钟）。

pluck 已经这么做了（`runlog.py:428-429`）：

```
{"t": round(time.monotonic() - self._t0, 4), "wall": round(time.time(), 4)}
```

**这是所有文件能对到同一条时间线上的基础。** 没有它，事后无法回答"拍这张照片的时候臂在哪、PLC 正在等什么"——而那正是记录存在的意义。

**为什么非要两个（2026-07-28 补足）** —— 这条不显然，值得写足：

| | 单调时钟 `t` | 墙钟 `wall` |
|---|---|---|
| 会不会跳 | **不会**。它只管"从某个零点起过了多久" | **会**。NTP 校时可以把它**往回拨** |
| 零点在哪 | 随意（开机、进程启动……），**本身没有意义** | 有意义：1970 年起的绝对时刻 |
| 能答什么 | **间隔**："这一步花了多久" | **绝对时刻**："几点发生的"、**跨机器/跨进程对齐** |

**只有墙钟会怎样**：NTP 一校时，`wall` 往回跳，于是"扎完的时刻 − 开始扎的时刻"**可能算出负数**。工厂机长时间运行、又常和时间服务器对时，这不是理论风险。**所有基于时间差的分析都可能出现无法解释的负值或跳变。**

**只有单调时钟会怎样**：零点是随意的，所以答不了"这次异常是几点发生的"（没法和产线其它系统、和人的记忆对上），也**没法跨机器比较**——两台机器的单调时钟零点毫无关系。

**少任何一个，都有一整类问题答不上来。**

> **一个旁证**：pluck 的 `_now()`（`runlog.py:428-429`）和框架的 `TrajectoryRecorder`（`telemetry/trajectory.py:247-253` 的 `t_wall` / `t_mono`）是**两边独立写的**，互不知情，却**都独立得出"要两个"**。同一个结论被独立发现两次，通常说明它是被问题本身逼出来的，不是风格偏好。

### 规则 2：自动盖上"这是哪次运行"

git sha（含 dirty 标记）、config 指纹、machine id、batch。

pluck 把这套叫 run-identity，并且**盖在每一行上**而不只是写进 `meta.json`（`runlog.py:371` 的 `_run_tags`，`:454` 每行 `rec.update`）。它的理由值得整段引用（`runlog.py:38-40`）：

> ``batch`` + ``machine_id`` are stamped on EVERY event row too (see ``_run_tags``): a row then **stands alone** when the index concatenates events across runs, **no join with meta.json needed**. One int + one short str per row = negligible.

**一行记录单独拎出来也要说得清自己是哪来的。** 几个月后把很多次运行的数据拼在一起分析时，你才分得清"这批效果好"是因为改了阈值还是换了代码。

§1.2 还发现了一个必须一般化的能力：**调用方要能追加自己的上下文**（pluck 在轨迹行上盖 `phase`/`round`/`attempt`）。运行身份是框架盖的，业务上下文是调用方盖的，**两者都要支持**。

### 规则 3：写什么由调用方定，框架**不解释含义**

框架不知道 `func=60` 是什么意思，也不该知道。

这条不是新立的，`telemetry/trajectory.py:19-23` 已经立过：

> **Arm-agnostic.** A *track* is just a namespace string. The recorder dumps *every* declared key under it, raw. It does not know what a "pose" is, does not extract translation, does not decompose orientation. Raw ``WorldBoard`` values go to disk; **interpretation belongs to the business layer that owns the meaning of those fields.**

配套的还有它的**无约定序列化**（`:24-26`）："the bytes you read back are the bytes the board held" —— 不做单位换算、不转欧拉角、不做任何"贴心"的加工。

### 规则 4：大载荷单独落文件，行里只留文件名

一帧 35 MB（相机 B，4024×3036×3），**塞不进 jsonl**。

写图片 → 落成独立文件 + 在行里记下文件名。pluck 的 `RunLogger.event` 就是这个形状（行里带 `frame` / `overlay` 两个相对路径字段）。

### 规则 5：**小的不丢，大的可丢** —— 而且调用方不用选

**（2026-07-28 重写。初稿这条只说"永不阻塞、永不抛"，把"丢不丢"留成了配置问题。现在它是框架级规则。）**

`write` 有两条路，**递给它什么东西就走哪条**：

| 递进去的 | 走哪条 | 保证 |
|---|---|---|
| **一行**（几百字节，序列化耗时微秒级） | **同步写 + 刷盘** | **绝不丢** |
| **大载荷**（一帧 35 MB，压缩 ~247 ms） | **异步、有界队列** | **可丢，但丢必记账** |

**为什么小的绝不丢**：**丢一行等于丢一个事实，而事实没法补。** "PLC 在 T 时刻回了 func=61"这件事，丢了就永远不知道它发生过——后面的分析会看到一个无法解释的空洞。而它的代价极小：几百字节、微秒级，同步写完全负担得起。**为这点代价去换一个"可能丢"的风险，是亏的。**

**为什么大的可以丢**：一帧 35 MB、压缩 247 ms。同步写就等于把生产线卡住 0.25 秒——那正是 EVO-012 整篇在拆的东西。而丢一帧的损失是**可度量、可接受**的：连拍序列少一帧，判定还能做；何况丢了会记账（规则 7），你知道自己丢了多少。

> **调用方不需要指定走哪条。** 这一条是刻意的：**"这个能不能丢"不该是每个调用点各自的判断**——判断错一次就是要么卡住产线、要么静默丢掉事实。载荷的大小和写入代价已经足够区分这两类，**框架据此自己选**。

**依据不是推理，是 pluck 现有实现本来就是这么分的**：

- **行**：`RunLogger.event` 在 `_write_lock` 里 `write` 完立刻 `flush`（`runlog.py:478-480`）——同步、落盘、没有队列、没有丢弃路径。
- **大载荷**：走 `AsyncSampleWriter`——独立线程 + 有界队列 + 丢最新 + 记账。

**两条路早就存在，只是没人把这个区分写成规则。** 本文把它写成规则。

**大载荷那条不要重新发明**：直接用 EVO-012 已经落地的 `sensor/delivery.py`——`ObservationQueue` / `DropPolicy` / `QueuedDelivery`，有界队列、按策略丢弃、丢了记账（`DropPolicy` 三值在 `:87-89`）。EVO-012 §2.3–2.8 已经把队列深度、丢弃策略、引用不失效、记账由外部主动取全部定完。**记录层是它的使用者，不是第二套实现。**

**共同底线**：两条路都**永不阻塞产线、永不把异常抛给调用方**。记录挂了不能把生产搞死。

### 规则 6：分账本

不同频率的东西写不同文件：决策一本、PLC 一本、臂的轨迹一本。

**理由是高频会把低频淹掉。** PLC 来往远比决策频繁——一次 poke 里决策只有几行，PLC 握手有几十行。混在一起，"扎中没扎中"就淹没在流水账里了。

pluck 已经用出了这个模式，而且把理由写在了 docstring 里（`runlog.py:23-26`）：

> ``frames/recognize/``: EVERY raw capture taken at the scan pose (not just the ones that led to a poke) ... **Kept in its own folder so the sparse decision keyframes above stay readable.**

同一份 docstring 里，`trajectory.jsonl`（高频背景流）/ `events.jsonl`（稀疏决策）/ `frames/` 也是各自分开的。**PLC 记录属于高频背景流，必须单开一本**，不能塞进 `events.jsonl`。

### 规则 7：丢了必须记一笔

pluck 的原话（`sample_writer.py:23-24`）：

> **丢了必须记进 events.jsonl** —— **静默丢帧离线才发现, 那是最坏的情形。**

为什么最坏：等你几天后翻数据发现少了帧，现场早就过去了，补不回来；而且你无法区分"没采到"和"采了但丢了"。

记账的形状沿用 EVO-012 §2.6：**丢帧时只加计数器，不通知任何人**；调用方在它本来就要停下来的时刻（一次 poke 结束）主动取走并清零，写**一行**记录。

> ⚠️ **一个必须避开的递归**：记账本身也是写。如果"我丢了一帧"这条记录也走那条已经满了的队列，就会自己吃掉自己。**所以计数器必须在内存里、记账走的是另一条（同步的、便宜的）路径**——这也正是"主动来取"而不是"回调推"的一个额外理由。
>
> 规则 5 改写之后这条更顺了：**记账本身是"一行"，走同步那条路，天然不会被大载荷队列的拥堵影响。**

### 规则 8：配置驱动，照既有约定；**`root` 必填**

**（2026-07-28 新增。）**

**照既有约定，不要新发明。** 框架里已经有 `from_config` 这个模式（`telemetry/trajectory.py:160`），而且它的两条讲究值得沿用：

- **认的键要和构造函数对齐**（`:153-154` 的 `_CONFIG_KEYS` 注释原话 "Keys recognised by ``from_config`` — mirror the constructor"）；
- **不认识的键直接报错**，理由写在 `:180-181`：让 YAML 里的拼写错误**在加载时大声失败**，而不是"用默认值静默地跑起来"。

**`root`（册子放在哪）必填，框架不给默认值。**

理由值得写清楚，因为"给个合理默认"看上去总是更友好：

> **框架给的默认一定是错的**——它不知道这台部署机器把盘挂在哪、哪个分区有空间、数据允不允许留在源码树里。
>
> 而**错的默认最坏的地方，不是它错，是它让程序能跑起来**：进程正常启动、日志正常写、一切看起来都对，然后把几十 GB 写到了不该写的地方。等有人发现时，磁盘满了或者数据在一个下次部署就会被覆盖的目录里。**相比之下，启动时因为缺配置而拒绝启动，是便宜得多的失败。**

`~/pluck-data` 这类值该由 **pluck 的 yaml** 提供（pluck 现在正是这么做的：`servo.save_dir`，见 §5.1）。

**身份分两半**：框架**能自己算的自己算**（git 版本 + dirty、配置指纹——它拿得到代码和配置），**算不出的由业务传入**（机器号、批次——这是部署和生产组织的信息，框架无从知道）。

#### ⚠️ `~` 展开：一个"框架该拿走什么"的最佳例子

这个坑 pluck 已经踩过并写下了完整分析（`runlog.py:95` `resolve_save_dir` 的 docstring），**框架该把它一次性拿走**，而不是让每个项目各自再踩一遍：

1. **`yaml.safe_load` 给回的是字面字符串 `"~/pluck-data"`**，`~` 没有任何魔法。
2. **`Path("~/pluck-data").mkdir()` 会高高兴兴地在当前工作目录下建一个名字就叫 `~` 的目录** —— 也就是**建回源码树里**，恰恰是"把数据挪出源码树"要避免的那件事。原文："creates a directory *named* ``~`` under the CWD (i.e. back inside the source tree — exactly what moving the data out was meant to stop)"。
3. **必须用 `os.path.expanduser`，不能用 `Path.expanduser`** —— 后者在取不到 home 目录时**抛 `RuntimeError`**。原文的判断很对：**"startup must not die over a log path"**（不该为了一个日志路径把启动搞死）。`os.path.expanduser` 在 `$HOME` 未设时会退到 `pwd` 记录，再不行就原样返回字符串。
4. **展开失败要大声警告** —— 因为那时数据真的会落进一个叫 `~` 的目录。pluck 的警告文案连"请在 yaml 里写绝对路径"都带上了。

**这四条合起来是一个很好的判据**：一件事如果**每个项目都必须做对、做错了症状还隐蔽**，它就该在框架里。路径展开正是这种事。

---

## 4. 包改名：`telemetry/` → `logbook/`

`src/autoweaver/telemetry/` 整体改名为 `src/autoweaver/logbook/`，`TrajectoryRecorder` 跟着搬。

**影响面已核实，很小**：

- 包内自引用 2 处（`telemetry/__init__.py:10`、`telemetry/trajectory.py:60`）
- 测试 1 处（`tests/telemetry/test_trajectory.py:10`）
- 一处 docstring 提及（`sensor/observation.py:139`）
- **`TrajectoryRecorder` 不是顶层导出**（`src/autoweaver/__init__.py` 零命中），所以 `from autoweaver import ...` 那条路不受影响
- **pluck 完全没有引用它**（全仓零命中），改名对 pluck 零风险
- `docs/architecture.md`、`docs/README.md` 都没提到它

### 4.1 ⚠️ `SCHEMA_ID` 是写进数据文件的字符串 —— **不能跟着改**

`telemetry/trajectory.py:60`：

```
SCHEMA_ID = "autoweaver.telemetry.trajectory/v1"
```

它不是内部常量——它被写进**每个轨迹文件的第一行** `_meta.schema`（`:279-283`）。

**已有的数据文件里带的是这个旧串。** 如果改名时顺手把它改成 `autoweaver.logbook.trajectory/v1`，那么：

- 老文件与新文件的 schema id 不同，但**格式完全一样**——离线读取工具会被迫处理一个没有实际差异的分叉；
- 更糟的是，如果读取方按 schema id 派发解析逻辑，老数据会变成"未知格式"。

**决定：保留旧串不动，并在旁边加一行注释说明为什么它和包名不一致。**

理由：schema id 标识的是**数据格式**，不是**代码位置**。包改名没有改变任何一个字节的输出格式，所以它不该 bump。为一次纯粹的重命名去分叉数据格式，是拿真实数据的可读性换代码的整洁——不划算。

> 这条的一般化教训值得记下：**任何被写进数据文件的字符串，都从此不再是代码的私事。** 改它要按数据迁移对待，不能按重构对待。

---

## 5. 本轮范围

### 5.1 做

**（2026-07-28 修订：范围整体收窄到"底座"，但**多了两样**——跨运行产物与清理，原先排在下一轮。）**

- **`Logbook`** —— 一次运行的册子：
  - 一个**目录**（`root` 必填，见规则 8）
  - **双时钟**（规则 1）+ **运行身份**（规则 2）+ **可变上下文**（§2.6）
  - **跨运行产物的落点**：`batch.json`（第几批）、`timing`、`latest.png` 这类**放在根目录下、不属于任何单次运行**的东西。它们今天在 pluck 里已经存在，而且和 `runs/` 共用同一个根——`resolve_save_dir` 的 docstring 说得很清楚，它存在的意义就是让"``runs/``, ``batch.json``, ``timing_last5.json`` and ``latest*.png`` **can never disagree about where they live**"。**一个根，一处解析**，这正是册子该管的。
  - **按天数清理旧运行**（`prune_old_runs` 的形状）。⚠️ 它的三条讲究要一并带过来，每条都是踩出来的：
    - **年龄看目录名，不看文件 mtime** —— 把数据拷到 U 盘或 rsync 一遍会重写 mtime，**备份不该让一次运行起死回生或者被判死刑**；
    - **看不懂的名字一律不删** —— 原文："Leaving junk is cheap; deleting data is not."；
    - **无人值守是它存在的前提** —— 工厂里没有任何人会来收拾，而一次运行约 335 MB。
- **`Scribe`** —— 绑定账本的写入口，唯一动词 `write`
- **`write` 的八条规则**（§3），其中规则 5 的**两条路（小的不丢 / 大的可丢）是框架级行为，不是配置项**
- **`telemetry/` → `logbook/`** 改名，`SCHEMA_ID` 保持不变

### 5.2 不做

- **pluck 侧任何改动。** 等感知层 + 记录层整体做完一起升（EVO-011 §5.0、EVO-012 §4.2 同此约定）。届时一次性处理：import 路径、`capture()` → `observe()`、`FreshFrameDahengCamera` 的静默陷阱、以及把 `RunLogger` 的底掏空换成 `Logbook` + `Scribe`。
- **`event` / `episode` / 四值结局那类大而全接口**（§2.5）。挑毛的四值结局（plucked / pluck_failed / abandoned / uncertain）、PLC 功能码的含义（60 是什么、61 是什么）、"看到几根毛""扎中没扎中"这些字段——**框架只管"怎么记"，不管"记的是什么意思"**（规则 3）。**框架提供底座，pluck 在上面包挑毛专用的一层。**
- **定时**（§2.4）。框架不提供、也不推荐任何一种节奏；`TrajectoryRecorder` 只是个可选现成件。

---

## 6. 考虑过但否决的

### 6.1 把记录层放进 `sensor/` —— 否决

讨论中一度提议把记录放进 `sensor/`，理由是"**PLC 和机械臂也都是传感器，它们的运行都要记录**"。

**这个直觉抓住了对的东西**：这些设备确实都在产出"某一刻发生了什么"，记录机制**应该只有一套**。这一半被采纳了，就是本文。

**但位置否决，两条理由：**

1. **与 EVO-011 刚用过的判据冲突。** `camera/` 挪进 `sensor/`，依据是**camera 就是一种 sensor**（`CameraBase(Sensor)` 的继承关系）。**记录器不是传感器，它是消费者**——它吃传感器产出的东西。按同一条判据，它不该住在里面。而且方向会反：`sensor/` 目前只依赖设备与观测，塞进记录等于让**设备层去依赖文件 IO、序列化、目录管理**。
2. **违反本仓库已经写下的法律。** `telemetry/__init__.py:6-7`："recording is downstream of the kernel, **not part of it. Keep it that way.**" 放进 `sensor/` 就是把记录变成 kernel 的一部分。

**附带否决"把 PLC / 臂改造成 `Sensor`"**：它们是**双向**的——你会命令机械臂，但你不会命令相机。硬套 `Sensor` 是对它们本性的错误描述。而且**不需要**：它们已经把状态发布到 `WorldBoard` 上了，记录器订阅 board 就能记——`TrajectoryRecorder` 现在干的正是这个。

### 6.2 ~~保留策略 / 磁盘占用 —— 本轮不做~~ → **改为本轮要做**

> **修订 · 2026-07-28**：初稿把这一节放在"否决/推后"里，理由是"先把写立起来，再谈清"。**这个划分被推翻了**——理由见 §5.1：册子放哪、留多久、跨运行的东西摆在哪，**都是"一本册子"这个概念自带的责任**，不是可选的附加功能。**内容移到 §5.1，本节只留推翻记录。**

原文的判断（"这显然是 `Logbook` 该管的事"）其实已经指向了正确答案，只是当时把它排到了下一轮。

⚠️ **另有一处初稿的事实错误，一并更正**：初稿写"**没人量过一次运行产生多少 GB**"。**这是错的**——`prune_old_runs` 的 docstring（`runlog.py:148` 起）里写着：

> Unattended factory operation: nothing ever comes to clean up, and **one run is ~335 MB**.

**量过，一次运行约 335 MB。** 而且那句话还给出了清理机制存在的真正理由：**工厂无人值守，没有任何人会来收拾。**

---

## 7. 本文最站不住脚的一条

**`Scribe` 的存在理由只有一条腿（§2.3）。**

它挣到位置靠的是"**绑定账本的写入口**"。但把这句话展开看，`Scribe` 大约等于：

```
scribe = 把 logbook.write 的"账本"参数预先填好
```

**这是一个 currying。** 它省下的，只是每次调用重复传一个字符串。而 `Observatory` 当初被否决，用的正是同款理由——"只是装配代码的活"。

**我认为它仍然应该存在**，因为绑定的很可能不只是账本名：一个 scribe 还该带上**默认字段**（比如它写的每一行都自动带 `worker="drill_vision"`），那就不只是省一个参数了。但**这一点目前还没有被真实用例确认**——本文写下它时，唯一确定要绑定的就是账本名。

所以这条的判断是：**理由成立，但薄。** 等 PLC 记录和视觉记录都真接上来之后，回头看一眼——如果那时 `Scribe` 上除了账本名还是什么都没有，就该按"无消费方不建"的规矩把它折回 `Logbook`，让调用方直接传账本名。

> **初稿列的第二弱项已解决**：初稿说"定时不是记录器的职责"**只是把问题移出去了、没有解决**。这一轮的答案是：**移出去就是答案**——框架不该替业务选节奏，两种节奏各有正当理由（§2.4）。这一条从"弱点"变成了"明确的边界"。

**第二弱：规则 5 的两条路是按"递进来的是什么"分派的，但"贵不贵"不是一种类型。**

规则 5 说得很干脆：一行走同步、大载荷走异步，**调用方不用选**。这个干脆是它的优点——但它假设**"是不是一行"能可靠地判断出"写起来贵不贵"**。

这个假设有缝：一个装了上万个键的 dict，**按类型是"一行"，按代价是"大载荷"**。它会走同步那条路，于是序列化和刷盘就发生在调用线程上——**恰好违反规则 5 自己承诺的"永不阻塞产线"**。

本文没有定"多大算大"这条线在哪。**这是刻意留的**（现在没有真实的中间尺寸载荷，划一条线就是猜），但要诚实标出来：**规则 5 的保证强度，取决于一个本文没有定义的边界。** 实现时至少该做到"**大到离谱的一行要能被发现并告警**"，而不是默默地把产线卡住。

**第三弱（是个内部不一致，实现时应当直接修掉）：`root` 必填的论证，同样适用于清理策略，而且更强。**

规则 8 论证 `root` 必须必填，用的是："**错的默认最坏在于它让程序能跑起来、然后把几十 GB 写到不该写的地方**"。

§5.1 现在把**按天清理旧运行**纳入了框架。那么按同一条逻辑：**一个错的保留天数，会让程序正常跑起来、然后把不该删的数据删掉。** 写错地方还能找回来，**删掉找不回来**——所以这条论证在清理上只会更强，不会更弱。

本文没有对保留天数提出同等要求。**建议实现时对齐**：保留天数同样必填、无默认；并且把 `prune_old_runs` 那两条保命性质当作硬约束（**年龄看目录名不看 mtime**、**看不懂的名字一律不删**）——一个会删用户数据的框架组件，保守是唯一可接受的姿态。
