[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "autoweaver"
version = "0.12.1"
description = "A framework for industrial vision inspection systems"
requires-python = ">=3.12"
dependencies = [
  "grpcio>=1.60",
  "numpy",
  "opencv-python",
  "protobuf>=4",
  "pymodbus>=3.11.4",
  "pyyaml>=6.0",
  "requests>=2.32.5",
  "scipy>=1.11",
]

[project.optional-dependencies]
yolo = ["ultralytics>=8.0"]
# Pin exactly: 2.0.2412.9191 imports numpy.compat.long (removed in numpy 2.0)
# and fails to import. 2.0.2407.9061 is the version proven on the capture rig.
daheng = ["iai-gxipy==2.0.2407.9061"]
websocket = ["websockets>=12"]

[tool.hatch.build.targets.wheel]
packages = ["src/autoweaver"]

[dependency-groups]
dev = [
    "grpcio-tools>=1.60",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[tool.pytest.ini_options]
markers = [
    "integration_safe: real-hardware tests that don't move the arm (run with -m integration_safe)",
    "integration: real-hardware tests that move the arm (run with -m integration; operator must be present)",
]
addopts = "-m 'not integration and not integration_safe' -p no:launch_pytest -p no:launch_testing -p no:launch_ros"
asyncio_mode = "auto"
